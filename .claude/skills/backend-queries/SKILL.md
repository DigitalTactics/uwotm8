---
name: Backend Queries
description: Write safe, performant database queries following best practices for SQL injection prevention, N+1 avoidance, and query optimization. Use this skill when writing or modifying database queries, ORM query chains, raw SQL statements, or repository/data-access layer code. Use when working on files like queries.py, repositories.py, services.py, dao.py, or any file that interacts with the database. Use when writing SELECT, INSERT, UPDATE, or DELETE operations, implementing eager loading, adding query caching, wrapping operations in transactions, or optimizing slow queries.
---

## When to use this skill:

- When writing new database queries or ORM query chains
- When modifying existing queries for performance or correctness
- When working on files such as queries.py, repositories.py, services.py, dao.py, or data-access layers
- When writing raw SQL statements or parameterized queries
- When fixing or preventing N+1 query problems
- When implementing eager loading or joins for related data
- When wrapping related database operations in transactions
- When adding query timeouts or caching for expensive queries
- When optimizing slow queries or adding strategic indexes

# Backend Queries

This Skill provides Claude Code with specific guidance on how to adhere to coding standards as they relate to how it should handle backend queries.

## Instructions

For details, refer to the information provided in this file:
[backend queries](../../../agent-os/standards/backend/queries.md)
