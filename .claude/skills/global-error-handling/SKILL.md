---
name: Global Error Handling
description: Implement robust error handling with user-friendly messages, specific exception types, centralized handling, and graceful degradation. Use this skill when writing or modifying error handling logic, try-catch blocks, exception classes, or error response formatting. Use when working on files like *.py, *.js, *.ts, *.rb, *.go, *.java, middleware files, exception handlers, or any code that catches, raises, or formats errors. Use when designing error boundaries, implementing retry strategies with backoff, validating preconditions, cleaning up resources in finally blocks, or creating custom exception types.
---

## When to use this skill:

- When writing try-catch/try-except blocks in any source code file
- When creating custom exception or error classes
- When implementing centralized error handling in controllers, middleware, or API layers
- When formatting error messages for end users
- When designing graceful degradation for non-critical service failures
- When implementing retry logic with exponential backoff for external service calls
- When ensuring resource cleanup (file handles, connections) in finally blocks
- When validating input and failing early with clear error messages
- When choosing between generic and specific exception types

# Global Error Handling

This Skill provides Claude Code with specific guidance on how to adhere to coding standards as they relate to how it should handle global error handling.

## Instructions

For details, refer to the information provided in this file:
[global error handling](../../../agent-os/standards/global/error-handling.md)
